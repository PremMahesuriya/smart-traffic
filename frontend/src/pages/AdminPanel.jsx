export default function AdminPanel() {
  return (
    <div>
      <h2>Admin Panel</h2>
      <p className="empty">
        Authentication (Phase 13) will add admin and operator login with JWT and role-based access.
      </p>
    </div>
  );
}
